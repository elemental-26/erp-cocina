import {
  APP_VERSION,
  CREADO_POR,
  CHANGELOG,
} from "../../services/appConstants";

export default function AdminAcercaDe({ primary }) {
  return (
    // Aquí pega exactamente el mismo JSX que ya tienes,
    // sin modificar absolutamente nada.
  );
}